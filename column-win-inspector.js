class ColumnWinInspector { 
    constructor(columnObject) { 
        
    }
    inspect() { 
        for (let i = 0; i < array.length; i++) { 
            //if square[i] === square[i+1] === square[i+2] === square[i+3]
            //then return playerNumber
            //else return 0
        }
    }
}

export default ColumnWinInspector;